name: Search Results

on:
  workflow_dispatch:
    inputs:
      quarry:
        description: 'Search query'
        required: true
        default: ''
        type: string
      branch_name:
        description: 'Branch to store search results'
        required: true
        default: 'search-results'
        type: string

jobs:
  search-and-publish:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          persist-credentials: true
          fetch-depth: 0

      - name: Prepare branch
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git fetch origin

          if git ls-remote --exit-code --heads origin ${{ inputs.branch_name }}; then
            git checkout ${{ inputs.branch_name }}
            git pull origin ${{ inputs.branch_name }}
          else
            git checkout --orphan ${{ inputs.branch_name }}
            git rm -rf . || true
            git commit --allow-empty -m "[bot] Initialize ${{ inputs.branch_name }} branch"
            git push --set-upstream origin ${{ inputs.branch_name }}
          fi

      - name: Setup Python
        run: |
          pip install --upgrade pip
          pip install phub[full]

      - name: Run search and generate result files
        run: |
          python3 << 'PYEOF'
          import json
          import os
          from phub import Client
          # Uncomment proxy block if you need WARP
          # from base_api.modules.config import config
          # from base_api.base import BaseCore
          # config.proxy = "socks5://127.0.0.1:1080"
          # core = BaseCore(config=config)
          # client = Client(core=core)

          client = Client()
          query = "${{ inputs.quarry }}"
          if not query.strip():
              raise ValueError("No search query provided")

          search = client.search(query=query, sort='views')
          successful = []
          limit = 30   # Reduced from 50 to avoid regex failures

          try:
              for video in search:
                  if len(successful) >= limit:
                      break
                  try:
                      info = video.dictify('all')
                      successful.append(info)
                  except Exception as e:
                      # Skip individual video that fails to dictify
                      print(f"Skipping video (error in dictify): {e}")
                      continue
          except Exception as e:
              # This catches RegexError during iteration (page parsing)
              print(f"Search iteration stopped early: {e}")
              print(f"Collected {len(successful)} results before error.")

          # Write results to text file (human-readable)
          with open('search_results.txt', 'w', encoding='utf-8') as f:
              f.write(f"Search query: {query}\n")
              f.write(f"Successful results: {len(successful)}\n\n")
              for idx, video_data in enumerate(successful, 1):
                  f.write(f"--- Result {idx} ---\n")
                  f.write(str(video_data))
                  f.write("\n\n" + "="*80 + "\n\n")

          # Also write JSON version for programmatic use
          with open('search_results.json', 'w', encoding='utf-8') as f:
              json.dump(successful, f, indent=2, ensure_ascii=False)

          print(f"Saved {len(successful)} results to search_results.txt and search_results.json")
          PYEOF

      - name: Generate README with links to results
        run: |
          cat > README.md << EOF
          # 🔍 Search Results

          | Item | Value |
          |------|-------|
          | **Query** | ${{ inputs.quarry }} |
          | **Date** | $(date -u) |
          | **Results file** | [Download search_results.txt](./search_results.txt) |
          | **JSON version** | [Download search_results.json](./search_results.json) |

          ## 📄 Preview (first 40 lines of text results)
          \`\`\`
          $(head -n 40 search_results.txt 2>/dev/null || echo "No results")
          \`\`\`
          EOF

      - name: Commit and push
        run: |
          git add README.md search_results.txt search_results.json
          git commit -m "[bot] Update search results for '${{ inputs.quarry }}'" || echo "No changes to commit"
          git push
